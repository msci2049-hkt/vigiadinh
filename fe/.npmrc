# Smoother DX with bleeding-edge majors: auto-install peers, don't hard-fail on
# peer mismatches (pnpm still prints warnings).
auto-install-peers=true
strict-peer-dependencies=false
# Keep dependencies honest (no phantom deps). Switch to "hoisted" only if a tool
# truly needs a flat node_modules.
node-linker=isolated
