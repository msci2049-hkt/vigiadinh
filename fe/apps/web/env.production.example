# ============================================================================
# env.production.example (KHÔNG chấm đầu — `.env.*` bị gitignore nuốt; cùng quy ước với be/deploy/README.md)
# → copy thành .env.production TRƯỚC KHI BUILD BẢN PROD.
# ⚠️ Vite nướng env vào bundle lúc BUILD, không đọc lúc chạy. Đổi bất kỳ giá trị
#    nào ở đây = PHẢI build lại. Deploy bundle cũ với env mới là không có tác dụng.
# ============================================================================

# API sau nginx + TLS. Xem BLOCKERS B-INFRA-1: domain này PHẢI để DNS-only trên
# Cloudflare (2 tầng subdomain → ngoài Universal SSL free → bật proxy là HTTP 000).
VITE_API_URL=https://api.familyhaven.mscilabs.com

# === rpId passkey — KHÔNG ĐẢO LẠI ĐƯỢC ===
# Chốt 2026-07-26. Authenticator băm rpId và nhúng vào credential NGAY LÚC TẠO:
# không API nào sửa được, không đường migrate. Đổi giá trị này sau khi có passkey
# đầu tiên = mọi passkey chết = người dùng mất đường vào ví, chỉ còn guardian recovery.
#
# KHÔNG dùng apex `mscilabs.com`: rpId là SCOPE. Apex nghĩa là passkey điều khiển
# tiền gọi được từ BẤT KỲ subdomain nào — một subdomain khác dính XSS là gọi được
# passkey ví. Scope hẹp thì TRÌNH DUYỆT tự từ chối, không phải chờ server kiểm.
VITE_PASSKEY_RP_ID=familyhaven.mscilabs.com

# === Stellar — BẮT BUỘC (env.ts hết default, fail-closed) ===
# RPC đi qua proxy BE (POST /rpc): FE không gọi thẳng provider, không mang key.
# Key provider (nếu có) nằm ở BE env STELLAR_RPC_API_KEY — xem be/deploy/env.production.example.
VITE_STELLAR_RPC_URL=https://api.familyhaven.mscilabs.com/rpc
# ĐỢT TESTNET 2026-07-27: production chạy TESTNET đồng bộ ba tầng (contracts +
# BE VPS + FE). Lên mainnet = đổi passphrase dưới + deploy lại 4 contract +
# thay 4 ID dưới CÙNG MỘT ĐỢT — passphrase mainnet:
#   Public Global Stellar Network ; September 2015
VITE_STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# === 4 giá trị chain TESTNET (đợt deploy 2026-07-27 — AUDIT-2026-07-25 §8) ===
# Toàn giá trị CÔNG KHAI. Nguồn sự thật: docs/security/AUDIT-2026-07-25.md §8;
# CI dùng đúng bộ này qua GitHub vars (deploy-fe.yml gate D1).
# origin-verifier PROD pin rpId=familyhaven.mscilabs.com + 3 origin web/api/extension.
VITE_ACCOUNT_WASM_HASH=2c19ee49d7f25a6a052e2dc16489e5b1b10afc322ff6a8a8483d0e408c796f35
VITE_WEBAUTHN_VERIFIER_ADDRESS=CAYJGXLB5J23S6DYFWS5VTFRVCEN5NLIUJCTO6GTM6LDQOYB7J6EOYQS
VITE_RECOVERY_REGISTRY_ADDRESS=CDDOCXZ3OWM5TAQCRBKELETTIHQZD5NL3SF564VMD63MVJOGFV27F4Q3
VITE_SAC_NATIVE=CDLZFC3SYJYDZT7K67VZ75HPJVIEUVNIXF47ZG2FB2RMQQVU2HHGCYSC
