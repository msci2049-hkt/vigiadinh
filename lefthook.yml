pre-commit:
  commands:
    gitleaks:
      run: node scripts/run-gitleaks.mjs protect --staged --redact --no-banner --config .gitleaks.toml
      fail_text: |
        gitleaks blocked a staged secret.
        Do not use --no-verify; remove the secret and commit again.
